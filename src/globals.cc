#include <globals.hh>
#include <LogHandler.hpp>

namespace global {
	StreamLog* serverLog;

    uint32_t    currTimeSim;

    SimOutputs_s   simOutputs;
    ExecOptions_s  execOptions;
    Params_s       params;
}

